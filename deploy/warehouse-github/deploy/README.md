# GitHub Deploy Key

## Usage
- Private key: github-deploy-key (keep secure!)
- Public key: github-deploy-key.pub (add to GitHub repo)

## Setup
1. Go to GitHub repo → Settings → Deploy keys
2. Add public key with write access
3. Use private key for GitHub Actions
